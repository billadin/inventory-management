import React from "react";
import { useLocation, useNavigate } from "react-router-dom";

const Pagination = ({handlePageChange, pages}) => {
    console.log(pages)

  return (
    <></>
  );
};

export default Pagination;
